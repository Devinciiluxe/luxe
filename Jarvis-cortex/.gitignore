# Credentials - never commit these
config/api_keys.json
config/pipeline_live.armed
config/pipeline_daemon.heartbeat
.env
*.env

# Python runtime artifacts
__pycache__/
*.py[cod]
venv/
.venv/

# Local editor / tooling state
.claude/
.DS_Store

# Local source backups
*.bak-*
